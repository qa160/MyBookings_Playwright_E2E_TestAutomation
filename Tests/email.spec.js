import { test } from '@playwright/test';

test.describe('Verify email notification is sent', async ({ page }) => {
  // Prerequisites: User is logged in, appointment is created
    //STEPS:
    //create a new inbox and get an API key
    //Install the MailSlurp SDK in your Node.js project.
    //Write a Playwright test script to check if an email is sent.
    //verify email content
    //delete the inbox after the test.
});